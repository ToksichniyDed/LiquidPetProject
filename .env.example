# order-postgres (проброшен на 5432)
ORDER_POSTGRES_DB=orders
ORDER_POSTGRES_USER=orders
ORDER_POSTGRES_PASSWORD=orders
ORDER_SERVICE_DATABASE_URL=postgresql://orders:orders@localhost:5432/orders?sslmode=disable

# worker-postgres (проброшен на 5433)
WORKER_POSTGRES_DB=worker
WORKER_POSTGRES_USER=worker
WORKER_POSTGRES_PASSWORD=worker
WORKER_SERVICE_DATABASE_URL=postgresql://worker:worker@localhost:5433/worker?sslmode=disable

# e2e-тесты
E2E_GATEWAY_URL=http://localhost:8081
